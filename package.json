{
  "name": "vuepress-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vuepress dev",
    "build": "vuepress build",
    "deploy": "npm run build && bash scripts/deploy.sh"
  }
}
